---
interface Props {
    id?: string
    class?: string
}

const { id, class: className } = Astro.props
---

<section id={id} class=`w-full mx-auto md:w-[740px] ${className}`>
    <slot />
</section>
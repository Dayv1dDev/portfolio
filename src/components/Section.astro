---
interface Props {
    id?: string
    class?: string
    title?: string
}

const { id, class: className, title } = Astro.props
---

<section id={id} class=`w-full mx-auto md:w-[740px] ${className}`>
    <h2 class="text-3xl font-semibold mb-4">{title}</h2>
    <slot />
</section>